# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'LCT2020' do
  use_frameworks!
    pod 'SwiftSoup'

  target 'LCT2020Tests' do
    inherit! :search_paths
    # Pods for testing
	
  end

end
